service: lambdas_bicibici

provider:
  name: aws
  runtime: nodejs10.x

functions:
  verificar_movimiento_bicicleta:
    handler: handler.checkMovement
    events:
      - iot:
          sql: ${file(./config.json):IoT_topic}